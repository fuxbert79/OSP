{
  "name": "RMS-Generate-Formblatt",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "rms-generate-formblatt",
        "responseMode": "lastNode",
        "options": {}
      },
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [250, 300],
      "webhookId": "rms-generate-formblatt",
      "id": "formblatt-webhook-1"
    },
    {
      "parameters": {
        "jsCode": "// Platzhalter-Implementierung\n// Die vollstaendige Implementierung erfordert LibreOffice\nconst input = $input.first().json;\nconst qaId = input.body?.qaId || 'UNKNOWN';\nconst formularTyp = input.body?.formularTyp || 'F_QM_14';\n\n// TODO: XLSX-Template laden, fuellen und zu PDF konvertieren\n// Erfordert:\n// 1. apt-get install libreoffice-calc libreoffice-writer\n// 2. pip install openpyxl\n// 3. XLSX-Templates in SharePoint unter /Formular-Vorlagen/\n\nreturn {\n  success: true,\n  message: 'PDF-Generierung vorbereitet (LibreOffice Installation erforderlich)',\n  qaId: qaId,\n  formularTyp: formularTyp,\n  info: 'Bitte installieren Sie LibreOffice und openpyxl fuer die vollstaendige Funktion'\n};"
      },
      "name": "Generate PDF (Placeholder)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [500, 300],
      "id": "generate-pdf-1"
    }
  ],
  "connections": {
    "Webhook": {
      "main": [[{"node": "Generate PDF (Placeholder)", "type": "main", "index": 0}]]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
